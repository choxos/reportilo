% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checklist.R
\name{get_checklist}
\alias{get_checklist}
\alias{new_checklist}
\title{Get a fillable reporting checklist}
\usage{
get_checklist(id)

new_checklist(id)
}
\arguments{
\item{id}{A \code{guideline_id} (or an unambiguous acronym). See
\code{\link[=reportilo_guidelines]{reportilo_guidelines()}}.}
}
\value{
An object of class \code{reportilo_checklist} (a data frame with columns
\code{item_no}, \code{section}, \code{item_text}, \code{response}), or \code{NULL} (invisibly) if the
guideline has no bundled checklist.
}
\description{
Return the checklist for a guideline as a fillable table: one row per item,
with an empty \code{response} column for you to record the page number (or other
response) where each item is addressed.
}
\examples{
chk <- get_checklist("prisma-2020")
head(chk)
chk$response[1:3] <- c("p1", "p2", "p2")
}
\seealso{
\code{\link[=validate_checklist]{validate_checklist()}}, \code{reportilo_export()}
}
