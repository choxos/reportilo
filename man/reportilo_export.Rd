% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{reportilo_export}
\alias{reportilo_export}
\title{Export a checklist or flow diagram to a file}
\usage{
reportilo_export(x, file, format = NULL, ...)
}
\arguments{
\item{x}{A \code{reportilo_checklist} or \code{reportilo_flowchart}.}

\item{file}{Output file path. Its extension sets the format unless \code{format} is
given.}

\item{format}{Optional explicit format (e.g. \code{"docx"}). Defaults to the file
extension.}

\item{...}{Passed to the underlying writer. For flow diagrams: \code{width} (image
pixel width) and \code{background} (\code{"white"} or \code{"transparent"}).}
}
\value{
The output \code{file} path, invisibly.
}
\description{
Write a filled \link[=get_checklist]{reportilo_checklist} or
\link[=new_flowchart]{reportilo_flowchart} to Word, Excel, an image, or CSV. The
output format is taken from \code{format}, or inferred from the file extension.
}
\details{
Supported formats:
\describe{
\item{Checklist}{\code{docx}, \code{xlsx}, \code{csv}.}
\item{Flow diagram}{\code{png}, \code{svg}, \code{pdf}, \code{docx}, \code{xlsx} (the counts), \code{csv}
(the counts).}
}

Word and Excel export need the suggested packages \code{officer} + \code{flextable} and
\code{openxlsx} respectively; image export needs \code{DiagrammeR}, \code{DiagrammeRsvg} and
\code{rsvg}.
}
\examples{
\dontshow{if (requireNamespace("officer", quietly = TRUE)) withAutoprint(\{ # examplesIf}
chk <- get_checklist("strobe")
reportilo_export(chk, tempfile(fileext = ".docx"))

fc <- set_counts(new_flowchart("prisma_2020"), identified_db = 1200)
reportilo_export(fc, tempfile(fileext = ".csv"))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=get_checklist]{get_checklist()}}, \code{\link[=new_flowchart]{new_flowchart()}}
}
